# خطة النقل الكامل إلى VPS

- [x] ملفات selfhost (Dockerfile, compose, nginx, sync scripts, migrate-storage)
- [x] `.dockerignore` لتسريع البناء ومنع تسريب ملفات محلية
- [x] سكربت تصدير الـschema/migrations للقاعدة الجديدة (`selfhost/migrate-db.sh`)
- [x] إنشاء مشروع Supabase الجديد + تشغيل migrations
- [x] نقل البيانات + Storage + المستخدمين (تم التحقق: 44 جدول، 80 سياسة، 7 buckets، 102 ملف، 4 حسابات، جدول رمز الاستلام شغال)

- [ ] المستخدم: git clone على الـVPS + `docker compose up -d --build`
- [ ] المستخدم: DNS + SSL + تعطيل جوب Bybit في Lovable + إلغاء النشر
- [ ] استبدال أسرار اتصال Fly بالقيم الجديدة وتجهيز أوامر النشر
- عدم إعادة تشغيل السحابة القديمة (Lovable Cloud) نهائيًا، والاكتفاء بالقاعدة الجديدة.
- [x] فصل كل إعدادات التشغيل عن القاعدة القديمة وتثبيت الاتصال بالقاعدة الجديدة فقط.

## إصلاح برنامج الموظف

- [x] تشخيص تسجيل المستخدم: المُثبّت كان يفتح البرنامج مخفيًا فورًا فيبدو أنه لم يعمل.
- [x] جعل أول تشغيل بعد التثبيت ظاهرًا، مع بقاء التشغيل مع ويندوز مخفيًا.
- [x] بناء ورفع الإصدار 3.1.24 للموقع، دون الاعتماد على مخزن القاعدة القديمة.
- [ ] نشر الموقع الحقيقي على Fly ليصل الإصدار الجديد للموظفين.

## دورة المعاملات الشهرية (UTC) — منفّذة
- `src/lib/monthly-cycle.server.ts`: بداية الدورة = 00:00 UTC لآخر يوم في الشهر (28/29/30/31 تلقائيًا)، وحذف فعلي لمعاملات الدورات السابقة من `bybit_card_txns` و`bybit_ledger` و`card_transactions`.
- جداول بيانات الشغل مستثناة بالكامل، وأي صف في `bybit_ledger` مرتبط بـ`work_txn_assignments` لا يُحذف.
- الحذف يعمل تلقائيًا مع كل مزامنة (`syncAllCardTxns`) وبعلامة في `site_settings.monthly_cycle_purge`.
- [x] إصلاح تنفيذ الحذف الفعلي: تصغير دفعات الطلبات، عدم تسجيل اكتمال عند أي خطأ، وإجبار إعادة المحاولة بالإصدار 2 قبل الاتصال بمزوّد المعاملات.

## تسريع تحميل بيانات لوحة الأدمن وصفحة الموظف
- [x] تقليل الطلبات المتكررة (من كل ثانيتين إلى كل 15 ثانية مع الاعتماد على التحديث اللحظي) + تخزين مؤقت لقوائم الموظفين/الشفتات + سقف صفوف للتحويلات والعملاء.

## نافذة تعديل البانر
- [x] مطابقة نافذة «تعديل البانر» مع الصورة المرجعية الداكنة، دون تغيير الحقول أو الحفظ أو الصفحة الخلفية.
- [ ] التحقق بصريًا داخل لوحة الإدارة بعد النشر؛ المعاينة المحلية لا تملك جلسة الإدارة أو اتصال قاعدة الموقع الحقيقي.

## تطبيق تعديلات القاعدة الجديدة
- [x] تجميع كل التعديلات المعلّقة في ملف واحد: `supabase/manual/apply-all.sql` (رمز الاستلام + عمود جهاز البانر).
- [ ] المستخدم: تشغيل الملف في SQL Editor على القاعدة الجديدة (لا يوجد اتصال مباشر بالقاعدة من هنا).

- [x] تتبع وإصلاح RLS في حفظ المنتجات: عزل ProductsTab، كل CRUD عبر Server Functions المصرح بها، رفع الصور بمفتاح UUID، وفحص TypeScript و33 اختبارًا وNode build بنجاح. اختبار المتصفح المصادق تعذر لأن المشروع يستخدم قاعدة خارجية لا توفر جلسة معاينة.
